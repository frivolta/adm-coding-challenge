import styled from "@emotion/styled";
import {noWrap} from "../../globals/theme";

// Common is for the excercise normally they should be different tested components
export const Tr = styled.tr`
  display: contents;
  ${noWrap};
`